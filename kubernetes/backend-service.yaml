apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: cost-optimization
  labels:
    app: backend-api
spec:
  selector:
    app: backend-api
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
